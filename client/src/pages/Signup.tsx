import{useState}from'react';import{Eye,EyeOff,ShieldCheck}from'lucide-react';import{Link,useNavigate}from'react-router-dom';import{api}from'../lib/api';import{Brand}from'./Login';
export default function Signup(){const nav=useNavigate();const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirmPassword,setConfirm]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');if(password!==confirmPassword){setError('Passwords do not match');return}setBusy(true);try{await api('/auth/signup-admin',{method:'POST',body:JSON.stringify({name,email,password,confirmPassword})});nav('/login',{replace:true,state:{created:true}})}catch(e){setError(e instanceof Error?e.message:'Account creation failed')}finally{setBusy(false)}}return <div className="flex min-h-screen items-center justify-center bg-[#f0f9ff] p-4"><form className="w-full max-w-md rounded border bg-white p-7 shadow" onSubmit={submit}><Brand/><div className="mb-5 flex gap-3 rounded border border-sky-100 bg-sky-50 p-3 text-xs text-sky-900"><ShieldCheck className="shrink-0" size={18}/><p>This creates the first workspace administrator. For security, signup closes automatically after the first user is created.</p></div><div className="grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2"><span className="label">Full name</span><input className="field" autoComplete="name" value={name} onChange={e=>setName(e.target.value)} minLength={2} required/></label><label className="sm:col-span-2"><span className="label">Work email</span><input className="field" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><Password label="Password" value={password} set={setPassword} show={show} toggle={()=>setShow(x=>!x)} autoComplete="new-password"/><Password label="Confirm password" value={confirmPassword} set={setConfirm} show={show} toggle={()=>setShow(x=>!x)} autoComplete="new-password"/></div><p className="mt-2 text-[11px] text-slate-400">Use at least 8 characters with upper-case, lower-case, and numeric characters.</p>{error&&<p className="mt-3 rounded bg-red-50 p-2 text-xs text-red-700">{error}</p>}<button className="btn btn-primary mt-5 w-full" disabled={busy}>{busy?'Creating workspace…':'Create administrator account'}</button><div className="mt-5 text-center text-xs text-slate-500">Already initialized? <Link className="font-semibold text-[#0284c7] hover:underline" to="/login">Return to sign in</Link></div></form></div>}
function Password({label,value,set,show,toggle,autoComplete}:{label:string;value:string;set:(v:string)=>void;show:boolean;toggle:()=>void;autoComplete:string}){return <label><span className="label">{label}</span><div className="relative"><input className="field pr-10" type={show?'text':'password'} autoComplete={autoComplete} value={value} onChange={e=>set(e.target.value)} minLength={8} required/><button type="button" onClick={toggle} className="absolute right-0 top-0 flex h-9 w-10 items-center justify-center text-slate-500" aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label>}
